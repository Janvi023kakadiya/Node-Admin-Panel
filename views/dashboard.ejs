<%- include('partials/header') %>

<div class="container mt-5">
  <div class="text-center mb-5">
    <h1 class="display-4 fw-bold">Dashboard</h1>
    <p class="lead">Welcome back, <span class="text-primary"><%= user.name %></span>!</p>
  </div>

  <div class="row justify-content-center">
    <div class="col-md-6 col-lg-4">
      <div class="card shadow-sm border-0">
        <div class="card-body text-center">
          <h5 class="card-title text-uppercase text-muted mb-3">Total Products</h5>
          <p class="display-3 fw-bold text-success mb-0"><%= totalProducts %></p>
          <i class="bi bi-box-seam text-success" style="font-size: 3rem;"></i>
        </div>
      </div>
    </div>
  </div>
</div>

<%- include('partials/footer') %>
