<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Admin Panel Home</title>
  <link rel="stylesheet" href="/css/bootstrap.min.css" />
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css"
    rel="stylesheet"
  />
</head>
<body>
  <%- include('partials/header') %>

  <div class="container mt-5">
    <div class="text-center">
      <h1 class="display-4 fw-bold mb-3">Welcome to the Admin Panel</h1>
      <p class="lead text-secondary mb-4">
        Manage users and products efficiently with ease.
      </p>

      <% if (!user) { %>
        <a href="/login" class="btn btn-primary btn-lg shadow-sm">
          <i class="bi bi-box-arrow-in-right me-2"></i> Login
        </a>
      <% } else { %>
        <a href="/user/dashboard" class="btn btn-success btn-lg shadow-sm">
          <i class="bi bi-speedometer2 me-2"></i> Go to Dashboard
        </a>
      <% } %>
    </div>
  </div>

  <%- include('partials/footer') %>
</body>
</html>
